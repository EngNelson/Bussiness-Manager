import React, { Fragment } from "react";
import AppBar from "../AppBar/AppBar";

const Dashboard = () => {
  return (
    <Fragment>
      <AppBar />
    </Fragment>
  );
};

export default Dashboard;
