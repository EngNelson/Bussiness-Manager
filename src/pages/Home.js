import React, { Fragment } from "react";
import AppBar from "../components/AppBar/AppBar";

const Home = () => {
  return (
    <Fragment>
      <AppBar />
    </Fragment>
  );
};

export default Home;
